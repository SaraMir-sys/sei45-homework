// The Calculator 1
//square number
const squareNumber = function(number){
const t = math.pow(number);
console.log('The result of squaring the number ${number} is ${t}');};
suareNumber (2);
//devide by 2
const halfNumber = function(No){const b = No/2 ;
console.log('${ b } is Half of {No}');
};
halfNumber (10);
//percent of no 1 of number 2
const percentOf = function(n1,n2){
const b = (n1/n2)*100 ;
console.log('${ n1 }is ${b} % of ${n2}');
};
percentof (4,7);
//area of circle rounded by 2
const areaOfCircle = function(radius){
const b =Math.round(Math.PI*math.pow(radius)*100)/100;
console.log('The area for a circle with radius ${radius} is ${b}');
};
areaOfCircle (2)
// The Calculator 2

const all = function(number){
//take half of the number
const halfNumber = function(number){const b = number/2};
//square the result
const t =  function(halfNumber){math.pow(halfNumber);};
//calculate area of circle
const areaOfCircle = function(radius){
const b =Math.round(Math.PI*math.pow(radius)*100)/100);};

//percentage of the end result
const percentOf = function(b,t){
const r = (b/t)*100 };
